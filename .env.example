# reM3 - reMarkable sync configuration
# Copy this file to .env and fill in your values

# Required: reMarkable tablet connection
RM_HOST=10.11.99.1
RM_USER=root
RM_PASSWORD=your_tablet_password_here

# Optional: Change base directory (default: ~/reM3)
# RM_BASE_DIR=/path/to/your/reM3/data

# Optional: Rendering settings
# RM_DPI=200

# Optional: Processing settings
# RM_WORKERS=3

# =================================================================
# AI Transcription Settings (choose one option below)
# =================================================================

# Option 1: OpenRouter with Qwen (RECOMMENDED - cheaper for testing)
# Uncomment these 3 lines and add your OpenRouter API key:
OPENAI_API_KEY=sk-or-v1-your_openrouter_key_here
OPENAI_BASE_URL=https://openrouter.ai/api/v1
OPENAI_MODEL=qwen/qwen2.5-vl-32b-instruct

# Option 2: OpenAI (higher quality, more expensive)
# OPENAI_API_KEY=sk-your_openai_key_here
# OPENAI_MODEL=gpt-4o

# Other OpenRouter models you can try:
# OPENAI_MODEL=qwen/qwen2.5-vl-7b-instruct          # Cheaper Qwen option
# OPENAI_MODEL=anthropic/claude-3-5-sonnet:beta     # Claude (if available)
# OPENAI_MODEL=google/gemini-pro-vision              # Gemini (if available)

# =================================================================
# 🔥 CRACKED MODE - Multi-model transcription + merge (EXPENSIVE!)
# =================================================================
# Uses multiple AI models + intelligent merge for best quality
# WARNING: Costs 3-5x more than single model transcription

# CRACKED_MODE=false                                 # Enable cracked mode
# CRACKED_MODELS=gpt-4o,anthropic/claude-3-5-sonnet:beta,qwen/qwen2.5-vl-32b-instruct
# CRACKED_MERGE_MODEL=gpt-4o                         # Model used to merge results
